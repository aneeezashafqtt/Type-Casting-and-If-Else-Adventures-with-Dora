/*#include <iostream>
using namespace std;

int main() {
	bool mapCorrect, swiperNear, bootsWarning, clueFound;

	cout << "Enter mapCorrect (SELECT 0 or 1): ";
	cin >> mapCorrect;

	cout << "Enter swiperNear (0 or 1): ";
	cin >> swiperNear;

	cout << "Enter bootsWarning (0 or 1): ";
	cin >> bootsWarning;

	cout << "Enter clueFound (0 or 1): ";
	cin >> clueFound;


	if (mapCorrect && !swiperNear && (bootsWarning || clueFound)) {
		cout << "Gate opened!" << endl;
	}
	else {
		cout << "Access denied!" << endl;
	}

	return 0;
} */