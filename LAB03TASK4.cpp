/*#include <iostream>
using namespace std;
int main()
{
	bool hasmap, hasboots;
	cout << "Does Dora have a map (1 if yes, 0 for no): ";
	cin >> hasmap;

	cout << "Does Dora have boots (1 if yes, 0 for no): ";
	cin >> hasboots;
	if (hasmap && hasboots)
		cout << "Dora is ready!";
	else if (hasmap || hasboots)
		cout << "Almost ready!";
	else
		cout << "Dora forgot essentials!";


	return 0;
 }*/